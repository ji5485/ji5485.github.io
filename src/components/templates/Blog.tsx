import React, { FunctionComponent } from 'react';
import styled from '@emotion/styled';

const BlogComponent = styled.div``;

const Blog: FunctionComponent<{}> = function ({}) {
  return <BlogComponent />;
};

export default Blog;
